@inherits Umbraco.Web.Mvc.UmbracoTemplatePage

@{
    var parent = CurrentPage.Parent;
}





<div class="list-group">
    @foreach (var item in parent.Children.Where("Visible"))
    {
        if (item.Id == CurrentPage.Id)
        {
            //This menu item is currently selected
            <a href="@item.Url" class="list-group-item active">@item.Name</a>
        }
        else
        {
            
            <a href="@item.Url" class="list-group-item"> @item.Name</a>
            
        }
    
    }
</div>

 <br/>
    <div class="heading-title heading-border-bottom">
        <h4>Links</h4>
    </div>
            <a target="_blank" href="/student-information/admission-inquiry-form/" class="btn btn-featured btn-round btn-blue" style="margin-bottom:12px">
                <span>Inquiry Form</span>
                <i class="et-clipboard btn-round"></i>
            </a>
            <a target="_blank" href="/admission/school-fees/" class="btn btn-featured btn-round btn-green" style="margin-bottom:12px">
                <span> School Fees</span>
                <i class="et-pricetags btn-round"></i>
            </a>
            <a target="_blank" href="/media/photo-gallery/" class="btn btn-featured btn-round btn-pink" style="margin-bottom:12px">
                <span>Photo Gallery</span>
                <i class="et-heart btn-round"></i>
            </a>
            <a target="_blank" href="/page-login-1" class="btn btn-featured btn-round btn-yellow" style="margin-bottom:12px">
                <span>Login</span>
                <i class="et-lock btn-round"></i>
            </a>
            
            
     
